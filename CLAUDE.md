@AGENTS.md

Read the meta repo's `AGENTS.md` (yegamble/vizra), the ADRs the change touches,
and the assigned execution plan on demand; do not preload this repository.
Before context compaction, preserve acceptance IDs, branch/SHA, modified files,
exact verification commands and their results, blockers, and the next concrete
action in the meta repo's `docs/plans/`. Never replace evidence with a
narrative success summary.
