# vendoring from a ref that does not exist in the core checkout
# $ node scripts/vendor-contract.mjs --from ../vizra-core --ref no/such/ref
# source: 549e589e2cc99a66cc8d0c01166d44bbd209db89 (+ the mutation this demonstration describes)
# node v22.14.0, Darwin arm64

fatal: Needed a single revision
vendor-contract: cannot read api/openapi.yaml at no/such/ref in /Users/yosefgamble/github/vizra/vizra-core
  Command failed: git rev-parse --verify no/such/ref^{commit}
Fetch the ref first, e.g. `git -C <checkout> fetch origin main`.

# exit=2 (expected 2)
