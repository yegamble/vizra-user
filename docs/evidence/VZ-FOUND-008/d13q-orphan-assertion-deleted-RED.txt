$ bash scripts/ci/check-e2e-lane.sh

::error::the e2e lane no longer tests what it claims to test:
  e2e/harness/test.ts no longer CALLS `formatOrphans`, so signals produced after the last test in a worker — an `afterAll` hook on a broken page — belong to no test and fail nothing. A worker-teardown throw is what makes that a red run. (it does not CALL `formatOrphans`.)
  These properties are invisible in a green run, which is why they are asserted here.

[exit 1]
