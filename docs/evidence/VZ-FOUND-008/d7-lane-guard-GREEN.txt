$ bash scripts/ci/check-e2e-lane.sh .github/workflows/e2e.yml

OK: .github/workflows/e2e.yml still drives the built image, keeps the coverage floor armed, uploads artifacts, and proves the harness does not ship.

[exit 0]
