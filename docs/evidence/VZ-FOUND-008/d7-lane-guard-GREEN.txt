$ bash scripts/ci/check-e2e-lane.sh .github/workflows/e2e.yml

OK: .github/workflows/e2e.yml still drives the built image, runs `npm run e2e` unconditionally, re-checks the coverage floor after it, and redacts artifacts before uploading them.

[exit 0]
