$ bash scripts/ci/check-e2e-lane.sh <repo>/docs/evidence/VZ-FOUND-008/e2e-mutant.yml

::error::the e2e lane no longer tests what it claims to test:
  no step uploads artifacts exactly as pinned; a red lane would be undiagnosable.
  job `e2e` step 14 (Upload Playwright artifacts) mentions `playwright-browsers.txt`, an artifact directory, `steps.redact` but is not byte-equal to the pinned `record_browsers` step in .github/e2e-pinned-steps.yml: `name` differs, `if` not in the pin, `with.name` not in the pin, `with.path` not in the pin, `with.retention-days` not in the pin, `with.if-no-files-found` not in the pin, `run` missing.
  These properties are invisible in a green run, which is why they are asserted here.

[exit 1]
