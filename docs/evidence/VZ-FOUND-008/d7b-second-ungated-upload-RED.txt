$ bash scripts/ci/check-e2e-lane.sh <repo>/docs/evidence/VZ-FOUND-008/e2e-mutant.yml

::error::the e2e lane no longer tests what it claims to test:
  job `e2e` step 16 (Upload Playwright artifacts (second, ungated)) mentions an artifact directory, the uploader `actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02` but is not byte-equal to the pinned `upload` step in .github/e2e-pinned-steps.yml: `name` differs, `if` differs, `with.name` differs, `with.path` differs, `with.retention-days` differs — it is not gated on `failure() && steps.redact.outcome == 'success'` — not gated on the redaction having SUCCEEDED. With two bare `failure()` conditions a redactor that exits non-zero still lets the unredacted tree be published, and a SECOND upload step publishes it even when the first one is correctly skipped; its `path:` must be exactly the pinned allowlist of literal paths; its `retention-days` must be the pinned value, at or under the ceiling.
  job `e2e` step 16 (Upload Playwright artifacts (second, ungated)) sets `retention-days: 14`, above the ceiling of 3. An artifact nobody downloaded in three days is an artifact nobody needed, and it stays readable by every collaborator on this private repository until it expires.
  job `e2e` step 16 (Upload Playwright artifacts (second, ungated)) uploads `playwright-report/`, which is not on the allowlist (test-results/, playwright-report/results.json, playwright-browsers.txt). Adding a path is a reviewed change to ALLOWED_UPLOAD_PATHS in scripts/ci/check-e2e-lane.mjs — in particular `playwright-report/index.html` carries a base64-embedded ZIP of the whole report dataset that no redactor here can reach.
  These properties are invisible in a green run, which is why they are asserted here.

[exit 1]
