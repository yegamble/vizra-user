$ bash scripts/ci/check-e2e-lane.sh <repo>/docs/evidence/VZ-FOUND-008/e2e-mutant.yml

::error::the e2e lane no longer tests what it claims to test:
  job `e2e` step 16 (Publish with some other uploader) mentions an artifact directory, the uploader `some-org/artifact-publisher@0000000000000000000000000000000000000000` but is not byte-equal to the pinned `upload` step in .github/e2e-pinned-steps.yml: `name` differs, `if` differs, `uses` differs, `with.name` differs, `with.path` differs, `with.retention-days` missing, `with.if-no-files-found` missing — it is not gated on `failure() && steps.redact.outcome == 'success'` — not gated on the redaction having SUCCEEDED. With two bare `failure()` conditions a redactor that exits non-zero still lets the unredacted tree be published, and a SECOND upload step publishes it even when the first one is correctly skipped; the only uploader is `actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02`; its `path:` must be exactly the pinned allowlist of literal paths; its `retention-days` must be the pinned value, at or under the ceiling; it does not set `if-no-files-found: error`. This step only runs on failure, so a wrong path would be a warning nobody ever reads.
  job `e2e` step 16 (Publish with some other uploader) uses `some-org/artifact-publisher@0000000000000000000000000000000000000000`, which is not on this workflow's pinned action allowlist. Every action that runs here can read the workspace and publish from it — `actions/cache` writes a blob other runs in this repository can read, and matches neither `upload` nor `artifact`. Adding an action is a reviewed change to ALLOWED_USES in scripts/ci/check-e2e-lane.mjs.
  job `e2e` step 16 (Publish with some other uploader) sets no `retention-days`, so it inherits the repository default (up to 90 days).
  These properties are invisible in a green run, which is why they are asserted here.

[exit 1]
