$ bash scripts/ci/check-e2e-lane.sh /Users/yosefgamble/github/vizra/vizra-user/docs/evidence/VZ-FOUND-008/e2e-mutant.yml

::error::the e2e lane no longer tests what it claims to test:
  the redaction step has no `id:`, so the upload step cannot be gated on whether it succeeded.
  the artifact upload is not gated on the redaction having SUCCEEDED. It must read `if: failure() && steps.<redact-step-id>.outcome == 'success'`; it reads `failure() && steps.redact.outcome == 'success'`. With two bare `failure()` conditions a redactor that exits non-zero still lets the unredacted tree be published.
  These properties are invisible in a green run, which is why they are asserted here.

[exit 1]
