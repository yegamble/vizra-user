$ bash scripts/ci/check-e2e-lane.sh <repo>/docs/evidence/VZ-FOUND-008/e2e-mutant.yml

::error::the e2e lane no longer tests what it claims to test:
  no step runs `bash scripts/ci/redact-artifacts.sh test-results playwright-report` exactly as pinned (redact-artifacts.sh). Playwright's traces carry raw query strings that e2e/harness/redact.ts cannot reach, and the page-snapshot gate lives in that script; uploading without it publishes both.
  job `e2e` step 13 (Redact URL query strings in the artifacts) mentions `redact-artifacts.sh`, an artifact directory but is not byte-equal to the pinned `redact` step in .github/e2e-pinned-steps.yml: `continue-on-error` not in the pin — the redaction step sets `continue-on-error`, which would report `success` however it exited — the gate below would then always open.
  These properties are invisible in a green run, which is why they are asserted here.

[exit 1]
