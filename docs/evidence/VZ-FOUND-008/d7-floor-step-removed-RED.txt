$ bash scripts/ci/check-e2e-lane.sh <repo>/docs/evidence/VZ-FOUND-008/e2e-mutant.yml

::error::the e2e lane no longer tests what it claims to test:
  no step runs `node scripts/ci/check-coverage-floor-ran.mjs` exactly as pinned. The in-process coverage floor lives in playwright.config.ts, which this pull request can edit; the floor must also be re-checked from the finished report.
  These properties are invisible in a green run, which is why they are asserted here.

[exit 1]
