$ bash scripts/ci/check-e2e-lane.sh

::error::the e2e lane no longer tests what it claims to test:
  e2e/harness/worker-guard.ts no longer CALLS `armCreationGuard`, so a spec can reach a context through `Browser.prototype.newContext` or launch a browser of its own, and nothing watches that page.
  These properties are invisible in a green run, which is why they are asserted here.

[exit 1]
