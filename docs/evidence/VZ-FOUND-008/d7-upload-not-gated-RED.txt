$ bash scripts/ci/check-e2e-lane.sh <repo>/docs/evidence/VZ-FOUND-008/e2e-mutant.yml

::error::the e2e lane no longer tests what it claims to test:
  no step uploads artifacts exactly as pinned; a red lane would be undiagnosable.
  job `e2e` step 14 (Upload Playwright artifacts) mentions `playwright-browsers.txt`, an artifact directory, the uploader `actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02` but is not byte-equal to the pinned `upload` step in .github/e2e-pinned-steps.yml: `if` differs — it is not gated on `failure() && steps.redact.outcome == 'success'` — not gated on the redaction having SUCCEEDED. With two bare `failure()` conditions a redactor that exits non-zero still lets the unredacted tree be published, and a SECOND upload step publishes it even when the first one is correctly skipped.
  These properties are invisible in a green run, which is why they are asserted here.

[exit 1]
