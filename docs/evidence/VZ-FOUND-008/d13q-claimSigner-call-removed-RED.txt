$ bash scripts/ci/check-e2e-lane.sh

::error::the e2e lane no longer tests what it claims to test:
  e2e/harness/test.ts no longer CALLS `claimSigner`, so nothing at RUNTIME distinguishes a test that went through the guard from one that reached `@playwright/test` directly. (it does not CALL `claimSigner`.)
  These properties are invisible in a green run, which is why they are asserted here.

[exit 1]
