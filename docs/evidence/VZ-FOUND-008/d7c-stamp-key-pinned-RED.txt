$ bash scripts/ci/check-e2e-lane.sh <repo>/docs/evidence/VZ-FOUND-008/e2e-mutant.yml

::error::the e2e lane no longer tests what it claims to test:
  no step runs the browser lane. Exactly one step must be byte-equal to the pinned `lane` step in .github/e2e-pinned-steps.yml (`run: npm run e2e` and nothing else) — a superstring such as `npm run e2e || true` launders the exit code, and deleting the step is silent.
  no step runs `node scripts/ci/harness-canary.mjs` exactly as pinned. Without it, neutering e2e/harness/browser-errors.ts while leaving its identifiers in place is silent: every other check in this lane stays green, because a guard that has stopped looking finds nothing to fail on.
  step 9 (Browser lane (desktop 1440, mobile 390)) sets `env:` (E2E_BASE_URL, VIZRA_E2E_STAMP_KEY). A step that is not pinned may set no environment: the lane guard allowlists env keys, and this step's list is empty.
  step 11 (The harness still fails a broken page (canary)) sets `env:` (E2E_BASE_URL, VIZRA_E2E_STAMP_KEY). A step that is not pinned may set no environment: the lane guard allowlists env keys, and this step's list is empty.
  a step sets `VIZRA_E2E_STAMP_KEY`. That key is minted per run so a spec cannot forge the harness stamp; pinning it to a known value would make the stamp forgeable.
  a step sets `VIZRA_E2E_STAMP_KEY`. That key is minted per run so a spec cannot forge the harness stamp; pinning it to a known value would make the stamp forgeable.
  job `e2e` step 9 (Browser lane (desktop 1440, mobile 390)) mentions `npm run e2e`, `E2E_BASE_URL` but is not byte-equal to the pinned `lane` step in .github/e2e-pinned-steps.yml: `env.VIZRA_E2E_STAMP_KEY` not in the pin — any other env key on the lane step changes what `npm run e2e` does.
  job `e2e` step 11 (The harness still fails a broken page (canary)) mentions `E2E_BASE_URL`, `harness-canary.mjs` but is not byte-equal to the pinned `canary` step in .github/e2e-pinned-steps.yml: `env.VIZRA_E2E_STAMP_KEY` not in the pin.
  These properties are invisible in a green run, which is why they are asserted here.

[exit 1]
