$ bash scripts/ci/check-e2e-lane.sh

::error::the e2e lane no longer tests what it claims to test:
  e2e/harness/test.ts no longer declares an automatic WORKER-scoped fixture (`{ scope: "worker", auto: true }`). A test-scoped listener is set up AFTER `beforeAll` has run, so a page opened and navigated in a hook is never observed.
  These properties are invisible in a green run, which is why they are asserted here.

[exit 1]
