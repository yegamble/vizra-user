$ bash scripts/ci/check-e2e-lane.sh /Users/yosefgamble/github/vizra/vizra-user/docs/evidence/VZ-FOUND-008/e2e-mutant.yml

::error::the e2e lane no longer tests what it claims to test:
  no step runs `node scripts/ci/harness-canary.mjs`. Without it, neutering e2e/harness/browser-errors.ts while leaving its identifiers in place is silent: every other check in this lane stays green, because a guard that has stopped looking finds nothing to fail on.
  These properties are invisible in a green run, which is why they are asserted here.

[exit 1]
